{
  "name": "mosquito-life",
  "version": "0.1.0",
  "description": "A 3D game where you play as a mosquito trying to feed on blood while avoiding being swatted.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "keywords": ["game", "3d", "threejs", "mosquito"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
